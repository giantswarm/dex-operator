oidc:
  customer:
    providers:
    - name: mock
      credentials: |-
        id: test
    - name: ad
      credentials: |-
        client-id: someid
        client-secret: verysecret
        tenant-id: anotherid
  giantswarm:
    providers:
    - name: mock
      credentials: |-
        name: name
        anothervalue: something

baseDomain: ""

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"
image:
  name: "giantswarm/dex-operator"
  tag: "[[ .Version ]]"
registry:
  domain: docker.io

pod:
  user:
    id: 1000
  group:
    id: 1000
